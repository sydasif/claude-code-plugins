{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "for file in $CLAUDE_FILE_PATHS; do if [[ \"$file\" == *.py ]]; then echo \"Formatting $file...\"; uv run ruff format \"$file\" && uv run ruff check --fix \"$file\"; fi; done"
          }
        ]
      }
    ]
  }
}